// Keep first line as-is ... at least don't try to put code here.

// This file needs to be placed in defaults/pref folder, according to docs
// the filename should start with an 'a'.

// Check the build script for doc links on what this 'obscure' thing does.
pref("general.config.obscure_value", 0);

// The general.config.filename pref registers our 'startup hook script'.
pref('general.config.filename', 'firefox-ethz.cfg');